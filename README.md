# 🚀 SQLiScanner v2.0: Автоматизированный Асинхронный Сканер Уязвимостей SQL-Инъекций

# !!! Сканировать чужие сайты незаконно! Сканируйте, только если вам дали на это согласие или сайт ваш !!!

## Обзор проекта

SQLiScanner v2.0 — это высокопроизводительный, асинхронный сканер, разработанный в рамках курсового проекта для автоматического обнаружения уязвимостей SQL-инъекций (SQLi) в GET-параметрах и HTML-формах веб-приложений.

Сканер реализует три основные техники атаки:
1. **Error-Based SQLi** (на основе ошибок СУБД).
2. **Boolean-Based Blind SQLi** (на основе логического ответа).
3. **Time-Based Blind SQLi** (на основе задержек ответа).

## 🛠️ Установка и Запуск

Для корректной работы сканера требуется Python 3.8+ и установка всех зависимостей.

### 1. Создание и Активация Виртуального Окружения

Настоятельно рекомендуется использовать виртуальное окружение для изоляции зависимостей.

| Шаг | Команда (Windows) | Команда (Linux/macOS) |
| :--- | :--- | :--- |
| **Создать** | `python -m venv venv_sqli` | `python3 -m venv venv_sqli` |
| **Активировать** | `venv_sqli\Scripts\activate` | `source venv_sqli/bin/activate` |

### 2. Установка Зависимостей

После активации окружения установите все необходимые библиотеки, перечисленные в `requirements.txt`:

```bash
pip install -r requirements.txt
```
### 3. Запуск
Для запуска просто запустите файл main.py с основным кодом. Следуйте инструкциям терминала.

### 4. Для сканирования сайтов с аутентификацией
Зарегистрируйтесь под своим аккаунтом 1 раз и нажмите кнопку F12. Откройте хранилище, выберете показать куки.
Откройте файл cookies.json и скопируйте в него только необходимые куки, указанные в файле. Остальные поля в файле не заполняйте